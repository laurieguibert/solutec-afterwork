{% extends 'base.html.twig' %}

{% block title %}Ajout consultant{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="/css/new_afterwork.css"/>
{% endblock %}

{% block body %}
    <div class="container">
        <div class="login-form">
            <div class="main-div">
                <div class="panel text-center">
                    <h1>Ajout d'un consultant</h1>
                    <p>Merci de remplir tous les champs.</p>
                </div>
                {{ include('user/_new_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        $('#user_lastName').focusout(function (){
           this.value = this.value.toUpperCase();
        });
    </script>
{% endblock %}
