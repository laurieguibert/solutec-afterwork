<form method="post" action="{{ path('mailing_list_delete', {'id': mailing_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');"  style="display: inline-block;">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mailing_list.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
