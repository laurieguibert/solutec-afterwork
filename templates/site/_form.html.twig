{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
    <a href="{{ path('admin_index') }}" class="btn btn-secondary">Retour</a>
{{ form_end(form) }}
