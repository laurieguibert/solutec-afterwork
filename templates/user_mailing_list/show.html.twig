{% extends 'base.html.twig' %}

{% block title %}UserMailingList{% endblock %}

{% block body %}
    <h1>UserMailingList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_mailing_list.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ user_mailing_list.createdAt ? user_mailing_list.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_mailing_list_index') }}">back to list</a>

    <a href="{{ path('user_mailing_list_edit', {'id': user_mailing_list.id}) }}">edit</a>

    {{ include('user_mailing_list/_delete_form.html.twig') }}
{% endblock %}
